# Copie para .env e preencha com seus valores
# cp .env.example .env

# Banco de dados
DB_USER=postgres
DB_PASSWORD=postgres_secret

# Ollama - URL do servidor local (padrão já configurado no docker-compose)
# Não é necessário alterar em uso normal com Docker Compose
# OLLAMA_BASE_URL=http://ollama:11434
